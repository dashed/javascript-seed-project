var assert, chai, expect, should;

chai = require('chai');

assert = chai.assert;

should = chai.should();

expect = chai.expect;

describe('something', function(done) {
  return describe("when given something", function() {
    return it("should do this", function() {});
  });
});
